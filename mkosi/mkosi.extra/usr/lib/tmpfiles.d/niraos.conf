# Runtime directory shared only by NiraOS IPC peers.
d /run/niraos 0770 root nira-ipc -

# NiraOS debug log directory (persistent writes; survives reboot)
# 0777 so the nira user session can write logs (compositor/shell/session).
d /var/log/niraos 0777 root root -
